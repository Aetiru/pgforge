{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la ventana principal y de las de workspace. Se agregan solo los que cada etapa necesita.",
  "windows": [
    "main",
    "ws-*"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "log:default",
    "notification:default"
  ]
}
